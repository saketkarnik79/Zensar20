Angular Router/Routing
	What is Angular Routing?
		Routing allows us to move from one part of the application to another part or one View to another View.
	What is Angular Router?
		The Router is separate module in Angular. It is in it's own dedicated library/package '@angular/router'
	By using Angular Router you can:
		Navigate to a specific view by typing an URL in the address bar.
		Pass optional parameters(query parameters) to the view.
		Bind clickable elements to the view and load the view when the user performs various application tasks.
		Handle back & forward buttons of the browser.
		Allows you to load the view dynamically.
		Protect the routes from unauthorized users using Route Guards.
	Components of Angular Router
		Router - A service that enables navigation from one component to the next component as users perform application tasks.
		Route - Tells Angular Router which view to display when user clicks a link or pastes a url into the browser address bar. Every route consists of 'path' and 'component'.
		Routes - an array of Route objects.
		RouterOutlet - a directive that serves as a placeholder, where Router should display the view.
		RouterLink - A directive that binds the HTML element to a Route.
		RouterLinkActive - A directive for adding or removing classes from an html element that is bound to a RouterLink.
		ActivedRoute - An object that represents the currenctly activated route associated with the loaded component.
		RouterState - The current state of the router includes a tree of the currently activated routes together with convinience methods for traversing the route tree.
		RouteLink Paramerters array - The parameters to the Route.
		
<!-- app.component.html -->
<app-header></app-header>
<router-outlet></router-outlet>
<app-footer></app-footer>


<!-- dashboard.component.html -->
<div class="sidebar"></div>
<div class="content">
  <router-outlet></router-outlet>
</div>


const routes: Routes = [
  {
    path: 'dashboard',
    component: DashboardComponent,
    children: [
      { path: 'reports', component: ReportsComponent },
      { path: 'settings', component: SettingsComponent }
    ]
  }
];


<router-outlet></router-outlet>
<router-outlet name="sidebar"></router-outlet>

const routes: Routes = [
  {
    path: 'dashboard',
    component: DashboardComponent,
    children: [
      { path: 'main', component: MainContentComponent },
      { path: 'menu', component: SidebarComponent, outlet: 'sidebar' }
    ]
  }
];

this.router.navigate([
  {
    outlets: {
      primary: ['dashboard', 'main'],
      sidebar: ['dashboard', 'menu']
    }
  }
]);

React JS
	Fundamentals
		What is ReactJS?
			React os like SQL for HTML or more specifically the DOM.
			It is a JavaScript library for building user interfaces. It provides a declarative library that keeps the DOM in sync with you code. It makes ir painless to create UIs on top of web standards.
		Why ReactJS?
			It is one of the most popular JavaScript libraries currently and it focuses on speedy rendering compared to the othe JavaScript libraries & frameworks with the help of Virtual DOM.
		Companies that use ReactJS
			Facebook
			Netflix
			Uber
			Twitter(Now X)
			Pinterest
			Airbnb
			Instagram
			reddit
			Instacart
			Microsoft
			and more.
			
			React is easy to learn with very lo concepts count.
			Ecosystem & Community
			Timeless Technology
		Comparison with Angular & VueJS
		ReactJS Architecture
			Where to Render?
				There are three place to Render?
					1. Server Side Rendering (SSR)
						Java Spring, ASP.NET, Ruby on Rails, PHP, Python
					2. Client Side Rendering (CSR) used mainly for SPAs
						React, Angular, Vue, AngularJS, Ember, Backbone
					3. Static Site Generation(SSG)
		Environment Setup
		ReactJS Elements
		JSX/TSX
		React Virtual DOM
		React Components
		React Project Setup
		
		Props
			Short form for properties. This is used when Parent component want to send data to Child Component.
		PropTypes (more important when coding with javascript)
			Data type information for Props.
		Lists in React
			function FruitList(props) {
			  const fruitListItems = props.fruits.map((fruit) => (
				<li key={fruit.id}>{fruit.name}</li>
			  ));
			  return <ul>{fruitListItems}</ul>;
			}

			const data = [
			  { id: 1, name: 'apple' },
			  { id: 2, name: 'orange' },
			  { id: 3, name: 'blueberry' },
			  { id: 4, name: 'banana' },
			  { id: 5, name: 'kiwi' },
			];

			ReactDOM.createRoot(document.getElementById('root')).render(
			  <FruitList fruits={data} />
			);

			function FruitListItem(props) {
			  return <li key="{props.fruit.id}">{props.fruit.name}</li>;
			}

			function FruitList(props) {
			  const fruitListItems = props.fruits.map((fruit) => (
				<FruitListItem key={fruit.id} fruit={fruit} />
			  ));
			  return <ul>{fruitListItems}</ul>;
			}

			const data = [
			  { id: 1, name: 'apple' },
			  { id: 2, name: 'orange' },
			  { id: 3, name: 'blueberry' },
			  { id: 4, name: 'banana' },
			  { id: 5, name: 'kiwi' },
			];

			ReactDOM.createRoot(document.getElementById('root')).render(
			  <FruitList fruits={data} />
			);
			
		Events in React
			<div id="root">
			  <button onclick="handleClick()">Click Me!</button>
			</div>
			
			//main.js
			function handleClick() {
			  console.log('clicked');
			}
			
			function handleClick(e, p) {
				console.log(e);
			  console.log('clicked'+p);
			}

			function Button() {
			  return <button onClick={handleClick(34)}>Click Me!</button>;
			}

			ReactDOM.createRoot(document.getElementById('root')).render(<Button />);
			
		Synthetic Events